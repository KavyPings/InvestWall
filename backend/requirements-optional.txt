# InvestWall backend — OPTIONAL heavy/network deps. Install to upgrade fidelity.
# Enable with the matching env flags in .env (see .env.example).

# ENABLE_TRANSFORMERS=1  -> real transformer text classifiers (DistilBERT / DeBERTa)
# ENABLE_IMAGE_MODEL=1   -> deepfake/AI image classifier (uses transformers + torch)
# For CPU-only torch install with:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
transformers==4.48.0
torch==2.5.1
timm==1.0.12
sentence-transformers==3.3.1
# Better face detection for the image/video deepfake pipeline (optional;
# falls back to multi-cascade OpenCV Haar if absent).
mediapipe==0.10.18

# ENABLE_WHISPER=1  -> audio speech-to-text for transcript scam analysis
faster-whisper==1.1.0

# ENABLE_QR=1  -> QR-code decoding in images / documents
pyzbar==0.1.9

# Production infra clients
redis==5.2.1
psycopg[binary]==3.2.3

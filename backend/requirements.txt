# InvestWall backend — single requirements file to RUN the app with all models.
#
# Installs everything the serving app needs, including the ML models (text
# transformer, deepfake/AI-image, Whisper). Each model still lazy-loads and the
# app degrades to rules/heuristics if a model can't load, so a partial install
# never crashes the server.
#
# NOTE: `backend/ml/requirements.txt` is a SEPARATE, offline dataset/training
# pipeline (CUDA build of torch, AWS Bedrock, etc.). You do NOT need it to run
# the app — only to retrain the MuRIL model.
#
# Heads-up: this pulls in PyTorch + Transformers (~2 GB). For a smaller CPU-only
# torch, install it first with:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# then run:  pip install -r requirements.txt

# --- Web / API ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
python-multipart==0.0.20
SQLAlchemy==2.0.36
python-dotenv==1.0.1

# --- Text NLP ---
spacy==3.8.3
tldextract==5.1.3

# --- Image / video / audio forensics ---
numpy==2.2.1
Pillow==11.1.0
piexif==1.1.3
opencv-python-headless==4.11.0.86
librosa==0.10.2.post1

# --- Authenticity / documents ---
dnspython==2.7.0
pypdf==5.1.0
python-docx==1.1.2

# --- ML models (text classifier, deepfake + AI-image, audio STT) ---
# PyTorch: plain install gives a CPU build on Windows; see the CPU-index note above.
torch
transformers==4.48.0
timm==1.0.12
sentence-transformers==3.3.1
faster-whisper==1.1.0

# --- Production infra clients (safe to install; local run uses SQLite/in-memory) ---
redis==5.2.1
psycopg[binary]==3.2.3

# --- Testing ---
pytest==8.3.4
httpx==0.28.1

# --- Optional extras (uncomment if you need them) ---
# Better face detection for deepfakes (else falls back to OpenCV Haar automatically):
# mediapipe==0.10.18
# QR-code decoding in images/PDFs (needs the zbar system library installed):
# pyzbar==0.1.9
